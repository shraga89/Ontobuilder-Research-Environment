<source url="http://www.woods.co.uk/index.asp" interurl="http://www.woods.co.uk/index.asp">
  <srcname>Woods Car Rental</srcname>
  <form>
    <attrgroup>
      <attr name="starting from">
        <domain format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
      </attr>
    </attrgroup>
    <attrgroup>
      <attr name="">
        <domain label="day" format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
        <domain label="month" format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
        <domain format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
        <domain label="hour" format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
        <domain label="min" format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
      </attr>
    </attrgroup>
    <attrgroup>
      <attr name="">
        <domain format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
      </attr>
    </attrgroup>
    <attrgroup>
      <attr name="" ename="Return branch">
        <domain format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
      </attr>
    </attrgroup>
    <attrgroup>
      <attr name="">
        <domain label="day" format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
        <domain label="month" format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
        <domain format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
        <domain label="hour" format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
        <domain label="min" format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
      </attr>
    </attrgroup>
    <attrgroup>
      <attr name="" ename="Age">
        <domain format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
      </attr>
    </attrgroup>
    <attrgroup>
      <attr name="We quote in UK AND.... " ename="quote in UK">
        <domain format="selection_list">
          <error> cannot extract the value because it's frameset</error>
        </domain>
      </attr>
    </attrgroup>
  </form>
</source>